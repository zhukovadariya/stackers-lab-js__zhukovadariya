// Дан массив чисел. Создайте функцию, которая увеличит каждое число на его позицию в массиве.
// Результат может содержать только однозначные числа, так что если добавление цифры с её позицией дает вам многозначное число, то должна быть возвращена только последняя цифра.
// (массивы будут содержать только числа)

// Пример:
//  [1, 2, 4]  -->  [2, 4, 7]
//  #[1+1, 2+2, 4+3]

//  [4, 6, 9, 1, 3]  -->  [5, 8, 2, 5, 8]
//  # [4+1, 6+2, 9+3, 1+4, 3+5] 
//  # 9+3 = 12  -->  2

function increaseNumbers(arr) {
    return arr.map((num, index) => {
      let sum = num + index + 1;
      while (sum > 9) {
        sum = sum % 10; // берем только последнюю цифру
      }
      return sum;
    });
  }

  // Пример
console.log(increaseWithPosition([1, 2, 4])); // [2, 4, 7]
console.log(increaseWithPosition([4, 6, 9, 1, 3])); // [5, 8, 2, 5, 8]
